{% extends 'base.html.twig' %}

{% block title %}Panier{% endblock %}

{% block body %}
    <h1>Panier</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ panier.id }}</td>
            </tr>
            <tr>
                <th>Adrlivraison</th>
                <td>{{ panier.adrlivraison }}</td>
            </tr>
            <tr>
                <th>Tel</th>
                <td>{{ panier.tel }}</td>
            </tr>
            <tr>
                <th>Modepaiment</th>
                <td>{{ panier.modepaiment }}</td>
            </tr>
            <tr>
                <th>Total</th>
                <td>{{ panier.total }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('panier_index') }}">back to list</a>

    <a href="{{ path('panier_edit', {'id': panier.id}) }}">edit</a>

    {{ include('panier/_delete_form.html.twig') }}
{% endblock %}
